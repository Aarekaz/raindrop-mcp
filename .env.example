# Raindrop.io API Access Token (for development/testing)
# Get your token from: https://app.raindrop.io/settings/integrations
RAINDROP_ACCESS_TOKEN=your_access_token_here

# OAuth Configuration (for production deployment)
# Get OAuth credentials from: https://raindrop.io/dev/apps
OAUTH_CLIENT_ID=your_oauth_client_id
OAUTH_CLIENT_SECRET=your_oauth_client_secret
OAUTH_REDIRECT_URI=https://your-server.vercel.app/auth/callback

# SECURITY: OAuth Allowed Redirect URIs (comma-separated)
# Only URLs in this list (or relative paths) will be accepted as redirect targets
# Example: https://myapp.com/dashboard,https://myapp.vercel.app/dashboard,/dashboard
OAUTH_ALLOWED_REDIRECT_URIS=https://your-server.vercel.app/dashboard,/dashboard

# Security
# Generate with: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=your_64_char_hex_encryption_key

# JWT Configuration (for OAuth Authorization Server)
# Generate with: openssl rand -base64 32
JWT_SIGNING_KEY=your_base64_jwt_signing_key
JWT_ISSUER=https://raindrop-mcp.anuragd.me
JWT_ACCESS_TOKEN_EXPIRY=3600  # 1 hour in seconds
JWT_REFRESH_TOKEN_EXPIRY=2592000  # 30 days in seconds

# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
API_KEY=optional_server_api_key

# CORS Configuration
CORS_ORIGIN=*

# Vercel KV (automatically set by Vercel when KV is linked)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
